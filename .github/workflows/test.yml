name: Test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: ${{ matrix.toxenv }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - python-version: "3.9"
            toxenv: django42-py39
          - python-version: "3.10"
            toxenv: django42-py310
          - python-version: "3.10"
            toxenv: django50-py310
          - python-version: "3.10"
            toxenv: django51-py310
          - python-version: "3.11"
            toxenv: django42-py311
          - python-version: "3.11"
            toxenv: django50-py311
          - python-version: "3.11"
            toxenv: django51-py311
          - python-version: "3.12"
            toxenv: django42-py312
          - python-version: "3.12"
            toxenv: django50-py312
          - python-version: "3.12"
            toxenv: django51-py312
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install tox
        run: pip install tox

      - name: Run tests (${{ matrix.toxenv }})
        run: tox -e ${{ matrix.toxenv }}

  lint:
    name: pycodestyle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install tox
        run: pip install tox

      - name: Run pycodestyle
        run: tox -e pycodestyle

  coveralls:
    name: Upload coverage to Coveralls
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install coverage coveralls Django markdown

      - name: Run tests with coverage
        run: coverage run manage.py test

      - name: Upload coverage to Coveralls
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: coveralls --service=github
