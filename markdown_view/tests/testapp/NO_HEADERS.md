Plain content with no Markdown headers at all, used to prove that rendering
a `MarkdownView` with `MARKDOWN_VIEW_TEMPLATE_USE_TOC` enabled doesn't error
out when there's nothing for the `toc` extension to build a table of
contents from.
