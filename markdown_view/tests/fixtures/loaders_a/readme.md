# Fixture used by markdown_view.tests.test_registry to prove that the
# markdown_view URL registry rebuilds when MARKDOWN_VIEW_LOADERS changes,
# even without ROOT_URLCONF/the resolver identity changing.
# "loaders_a" version.
